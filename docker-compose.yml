services:
  bot:
    build: .
    container_name: telegram-channel-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/bot.db
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
